import {makeStyles} from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    movieList: {
        maxWidth: 1335,
        margin: '0 auto',
        display: 'flex',
        flexWrap: 'wrap'
    },
}))

export default useStyles
